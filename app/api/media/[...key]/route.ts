import { env } from "cloudflare:workers";
import { get } from "@vercel/blob";
import { blobStoreToken, isBlobStoreConfigured } from "@/lib/blob-store";
type RuntimeEnv={MEDIA?:R2Bucket};type Context={params:Promise<{key:string[]}>};
export async function GET(request:Request,{params}:Context){const {key}=await params;const pathname=key.join("/");if(isBlobStoreConfigured()){const result=await get(pathname,{access:"private",ifNoneMatch:request.headers.get("if-none-match")||undefined,token:blobStoreToken()});if(!result)return new Response("Not found",{status:404});if(result.statusCode===304)return new Response(null,{status:304,headers:{etag:result.blob.etag,"cache-control":"private, no-cache"}});return new Response(result.stream,{headers:{"content-type":result.blob.contentType||"application/octet-stream","x-content-type-options":"nosniff",etag:result.blob.etag,"cache-control":"private, no-cache"}});}const bucket=(env as unknown as RuntimeEnv).MEDIA;if(!bucket)return new Response("Not found",{status:404});const object=await bucket.get(pathname);if(!object)return new Response("Not found",{status:404});const headers=new Headers();object.writeHttpMetadata(headers);headers.set("etag",object.httpEtag);headers.set("x-content-type-options","nosniff");return new Response(object.body,{headers});}
