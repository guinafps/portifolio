import { AdminShell } from "@/components/admin/admin-shell";import { SettingsForm } from "@/components/admin/settings-form";import { getSettings } from "@/lib/data";import { requireAdmin } from "@/lib/admin-auth";export const dynamic="force-dynamic";export default async function Settings(){const [user,settings]=await Promise.all([requireAdmin("/admin/settings"),getSettings()]);return <AdminShell user={user} eyebrow="PREFERÊNCIAS" title="Configurações"><SettingsForm settings={settings}/></AdminShell>}
