import { AdminShell } from "@/components/admin/admin-shell";import { MessagesManager } from "@/components/admin/messages-manager";import { getMessages } from "@/lib/data";import { requireAdmin } from "@/lib/admin-auth";export const dynamic="force-dynamic";export default async function Messages(){const [user,messages]=await Promise.all([requireAdmin("/admin/messages"),getMessages()]);return <AdminShell user={user} eyebrow="CAIXA DE ENTRADA" title="Mensagens"><MessagesManager initialMessages={messages}/></AdminShell>}
