import { AdminShell } from "@/components/admin/admin-shell";import { ProjectsManager } from "@/components/admin/projects-manager";import { getAllProjects } from "@/lib/data";import { requireAdmin } from "@/lib/admin-auth";export const dynamic="force-dynamic";export default async function AdminProjects(){const [user,projects]=await Promise.all([requireAdmin("/admin/projects"),getAllProjects()]);return <AdminShell user={user} eyebrow="CONTEÚDO" title="Projetos"><ProjectsManager initialProjects={projects}/></AdminShell>}
