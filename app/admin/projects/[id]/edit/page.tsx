import { notFound } from "next/navigation";import { AdminShell } from "@/components/admin/admin-shell";import { ProjectForm } from "@/components/admin/project-form";import { getProjectById } from "@/lib/data";import { requireAdmin } from "@/lib/admin-auth";export const dynamic="force-dynamic";export default async function EditProject({params}:{params:Promise<{id:string}>}){const {id}=await params;const [user,project]=await Promise.all([requireAdmin(`/admin/projects/${id}/edit`),getProjectById(Number(id))]);if(!project)notFound();return <AdminShell user={user} eyebrow="EDITAR CONTEÚDO" title={project.title}><ProjectForm project={project}/></AdminShell>}
